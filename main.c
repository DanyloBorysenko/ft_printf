#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include "ft_printf.h"

int	main(void)
{
	// char *ptr = "Hello";
	// int expeacted = printf(" decimal d %d, integer i %i, unsigned u %u, %%, x %x, X %X, s %s, p %p, c %c\n", 10, -10, 0, INT_MAX, INT_MAX, ptr, ptr, 'A');
	// int actual = ft_printf(" decimal d %d, integer i %i, unsigned u %u, %%, x %x, X %X, s %s, p %p, c %c\n", 10, -10, 0, INT_MAX, INT_MAX, ptr, ptr, 'A');
	// printf("expected : %d, actual : %d\n", expeacted, actual);
	// char	*ft_itoa_p(void *p);
}
